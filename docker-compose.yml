version: '3'
services:
  postgres:
    container_name: users-db
    image: postgres:14
    environment:
      POSTGRES_DB: users
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgresPassword
    ports:
      - "5432:5432"